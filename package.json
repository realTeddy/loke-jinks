{
  "name": "loke-jinks",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.0",
    "@cloudflare/workers-types": "^4.20250310.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.9",
    "wrangler": "^4.77.0"
  }
}
